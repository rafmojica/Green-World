export function setup_renderer(renderer){
    renderer.setClearColor("#61daff");
    renderer.setSize(window.innerWidth, window.innerHeight);
};

export function other_function(){

}

// module.exports = { setup_renderer };